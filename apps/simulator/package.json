{
  "name": "simulator",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist/",
    "lint": "eslint --max-warnings 0",
    "start": "tsx watch --clear-screen=false src/app.ts"
  }
}
